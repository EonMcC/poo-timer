# this is an auto generated file. This will be overwritten
query GetUser($id: String!) {
  getUser(id: $id) {
    id
    email
    name
    signupDate
    firstPooDate
    hourlyRate
    totalPaid
    currency
    longestPooTime
    numberOfPoos
    totalPooTime
  }
}
query ListUsers(
  $filter: TableUserFilterInput
  $limit: Int
  $nextToken: String
) {
  listUsers(filter: $filter, limit: $limit, nextToken: $nextToken) {
    items {
      id
      email
      name
      signupDate
      firstPooDate
      hourlyRate
      totalPaid
      currency
      longestPooTime
      numberOfPoos
      totalPooTime
    }
    nextToken
  }
}
query GetPoo($pooId: String!) {
  getPoo(pooId: $pooId) {
    pooId
    userId
    duration
    createdAt
  }
}
query ListPoos($filter: TablePooFilterInput, $limit: Int, $nextToken: String) {
  listPoos(filter: $filter, limit: $limit, nextToken: $nextToken) {
    items {
      pooId
      userId
      duration
      createdAt
    }
    nextToken
  }
}
